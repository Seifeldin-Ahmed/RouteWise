/**
 * Where the Spring Boot backend lives. The backend allows this app origin through CORS
 * via routing.cors.allowed-origins in application.properties.
 */
export const API_BASE_URL = 'http://localhost:8080';
